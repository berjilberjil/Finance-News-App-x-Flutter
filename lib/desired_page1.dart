import 'package:flutter/material.dart';

class DesiredPage1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Desired Page 1'),
      ),
      body: Center(
        child: Text('This is Desired Page 1'),
      ),
    );
  }
}
